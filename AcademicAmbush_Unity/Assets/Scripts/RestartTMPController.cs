using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;
public class RestartTMPController : MonoBehaviour
{
    public TMP_Text restart;
    // Start is called before the first frame update
    void Start()
    {
        restart.text = "";
    }
}
